[project]
name = "agent-engineering-playbook"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["self-cli"]

[tool.uv.sources]
self-cli = { path = "../self", editable = true }
